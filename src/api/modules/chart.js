import * as RequestApi from '../request';

export function getListIdeaHaveMostLike(url, data) {
    return RequestApi.getAll(url, data);
}

export function getListIdeaHaveMostView(url, data) {
    return RequestApi.getAll(url, data);
}


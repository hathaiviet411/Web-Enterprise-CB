describe('Example', () => {
    it('Test Login', () => {
        expect(1 + 1).toEqual(2);
    });

    it('Test Dashboard', () => {
        expect(1 + 1).toEqual(2);
    });

    it('Test User Management', () => {
        expect(1 + 1).toEqual(2);
    });

    it('Test Idea Management', () => {
        expect(1 + 1).toEqual(2);
    });

    it('Test Cat', () => {
        expect(1 + 1).toEqual(2);
    });
});

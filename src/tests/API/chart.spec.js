
describe('Testing API chart', () => {
    beforeEach(() => {
    });


    test('Testing API create chart', () => {
        const chart = {}
        expect(chart).toBe(chart)
    })

    test('Testing API get chart', () => {
        const chart = {}
        expect(chart).toBeDefined()
    })

    test('Testing API update chart', () => {
        const chart = {}
        expect(chart).toBe(chart)
    })

    test('Testing API remove chart', () => {
        const chart = {}
        expect(chart).toBe(chart)
    })
})
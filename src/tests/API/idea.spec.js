
describe('Testing API idea', () => {
    beforeEach(() => {
    });


    test('Testing API create idea', () => {
        const idea = {}
        expect(idea).toBe(idea)
    })

    test('Testing API get idea', () => {
        const idea = {}
        expect(idea).toBeDefined()
    })

    test('Testing API update idea', () => {
        const idea = {}
        expect(idea).toBe(idea)
    })

    test('Testing API remove idea', () => {
        const idea = {}
        expect(idea).toBe(idea)
    })
})
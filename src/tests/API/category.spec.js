
describe('Testing API category', () => {
    beforeEach(() => {
    });


    test('Testing API create category', () => {
        const category = {}
        expect(category).toBe(category)
    })

    test('Testing API get category', () => {
        const category = {}
        expect(category).toBeDefined()
    })

    test('Testing API update category', () => {
        const category = {}
        expect(category).toBe(category)
    })

    test('Testing API remove category', () => {
        const category = {}
        expect(category).toBe(category)
    })
})

describe('Testing API department', () => {
    beforeEach(() => {
    });

    test('Testing API create department', () => {
        const department = {}
        expect(department).toBe(department)
    })

    test('Testing API get department', () => {
        const department = {}
        expect(department).toBeDefined()
    })

    test('Testing API update department', () => {
        const department = {}
        expect(department).toBe(department)
    })

    test('Testing API remove department', () => {
        const department = {}
        expect(department).toBe(department)
    })
})